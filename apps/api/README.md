# apps/api (Phase 2 — placeholder)

Deliberately thin backend: auth, billing (Stripe), report history (metrics, not images),
licenses. No heavy image processing in the base scenario. The stack (Go vs NestJS) is
decided *before* Phase 2, not now. There is no backend at all in Phase 1.

No code yet. See `docs/AGENT_GUIDE.md` (§2, §3, §6).
