# workers/fix-worker (Phase 2 — placeholder)

Server-side heavy fix jobs (repack / transcode) off the request path, part of the paid Pro
flow in Phase 2. Note: in Phase 1 the *analysis* runs client-side in a browser Web Worker
inside `apps/web` — this server worker is a separate thing for the optimizer.

No code yet. See `docs/AGENT_GUIDE.md` (§6, Phase 2).
