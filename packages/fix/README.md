# @asset-doctor/fix (Phase 2 — placeholder)

The optimizer: MaxRects repacking, trim, power-of-two, and format transcoding
(libwebp/libavif via WASM). Not built in Phase 1 — Milestone 1 shows the value of a fix
with numbers and the emptiness overlay, never a faked "after" result.

No code yet. See `docs/AGENT_GUIDE.md` (§6, Phase 2).
